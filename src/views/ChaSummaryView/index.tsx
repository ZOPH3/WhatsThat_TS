import React from 'react';
import ChatSummaryViewContainer from './ChatSummaryViewContainer';

const ChatSummaryView = () => {
  return <ChatSummaryViewContainer />;
};

export default ChatSummaryView;